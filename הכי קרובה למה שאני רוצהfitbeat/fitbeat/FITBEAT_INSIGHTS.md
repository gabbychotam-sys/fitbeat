# 📱 FitBeat v4.3.1 - קובץ תובנות מלא ומשולב

**תאריך עדכון:** 15 ינואר 2025  
**גרסה:** 4.3.1 (עם כל התיקונים והשיפורים)  
**רזולוציה:** 280×280 פיקסלים  
**מכשיר:** Garmin Fenix 8 Solar 51mm

---

## 🎯 סימולציה רשמית - **זה מה שצריך לחקות!**

### לינק לסימולציה:
```
https://fenix-preview.preview.emergentagent.com/fitbeat-interactive.html
```

**⚠️ חשוב ביותר:** הסימולציה הזו היא המקור האמיתי! כל שינוי בקוד חייב להתאים לה בדיוק.

---

## 🔨 פקודת Build הנכונה

### Windows (הנפוץ ביותר):
```cmd
cd "C:\Users\gabbyc\Desktop\fitbeat"
"%APPDATA%\Garmin\ConnectIQ\Sdks\connectiq-sdk-win-8.4.0-2025-12-03-5122605dc\bin\monkeyc.bat" -o FitBeat.prg -f monkey.jungle -y developer_key.der -d fenix8solar51mm
```

### אם התיקייה בנתיב אחר או תיקייה כפולה:
```cmd
cd "C:\Users\gabbyc\Desktop\fitbeat\fitbeat"
"%APPDATA%\Garmin\ConnectIQ\Sdks\connectiq-sdk-win-8.4.0-2025-12-03-5122605dc\bin\monkeyc.bat" -o FitBeat.prg -f monkey.jungle -y developer_key.der -d fenix8solar51mm
```

### macOS:
```bash
cd ~/Desktop/fitbeat
~/Library/Application\ Support/Garmin/ConnectIQ/Sdks/connectiq-sdk-mac-8.4.0/bin/monkeyc -o FitBeat.prg -f monkey.jungle -y developer_key.der -d fenix8solar51mm
```

**הסבר דגלים:**
- `-o FitBeat.prg` - שם קובץ הפלט
- `-f monkey.jungle` - קובץ קונפיגורציה של הפרויקט
- `-y developer_key.der` - מפתח חתימה (חובה!)
- `-d fenix8solar51mm` - מכשיר יעד (Fenix 8 Solar 51mm)

---

## 🚨 כללי ברזל - חובה לקרוא ראשון!

### כלל 1: מבנה קובץ ZIP נכון
```
fitbeat/                          ← תיקייה ראשית בשם fitbeat
├── source/
│   ├── FitBeatApp.mc             (630 שורות - לוגיקה)
│   └── FitBeatView.mc            (487 שורות - UI)
├── resources/
│   ├── drawables/
│   │   ├── drawables.xml
│   │   └── launcher_icon.png
│   └── strings/
│       └── strings.xml
├── manifest.xml                   (הגדרות אפליקציה)
├── monkey.jungle                  (קונפיגורציה)
├── developer_key.der              (מפתח חתימה - חובה!)
├── BUILD_INSTRUCTIONS.md
└── README.md
```

**⚠️ חשוב ביותר:** 
- ה-ZIP חייב להכיל תיקייה **אחת** בשם `fitbeat`
- **לא** תיקייה כפולה `fitbeat/fitbeat`
- כשהמשתמש חולץ, הוא אמור לקבל תיקייה אחת בשם `fitbeat`

### כלל 2: הפקודה חייבת לרוץ מהתיקייה הנכונה
```cmd
# בדוק איפה אתה:
dir

# אתה צריך לראות:
manifest.xml
monkey.jungle  
developer_key.der
source (תיקייה)
resources (תיקייה)

# אם לא - אתה לא במקום הנכון!
```

### כלל 3: שגיאה נפוצה - "jungle file doesn't exist"
**פתרון:**
```cmd
# אם אתה רואה תיקייה fitbeat, היכנס פנימה:
cd fitbeat

# בדוק שוב:
dir

# עכשיו הרץ את ה-build
```

### כלל 4: טעויות קוד נפוצות - **אסור לחלוטין!**
- ❌ `getFontAscent` - לא קיים! (השתמש ב-`getFontHeight`)
- ❌ `ActivityMonitor.getInfo().currentHeartRate` - לא עובד! (השתמש ב-`Activity.getActivityInfo().currentHeartRate`)
- ❌ מיקומים בפיקסלים קבועים - `var y = 35;` (השתמש ב-`var y = h / 8;`)
- ❌ overlay על המסך - לא נתמך! (הודעות במסך נפרד)
- ❌ `Application.Properties` - לא קיים! (השתמש ב-`Application.Storage`)

### כלל 5: גדלי פונטים - **Garmin Fenix 8 (280×280)**
**אלה הגדלים הרשמיים - אל תשנה אותם!**

| קבוע Garmin | גודל (פיקסלים) | שימוש ב-FitBeat |
|-------------|----------------|------------------|
| `FONT_XTINY` | **22px** | תוויות (mi/km/steps) |
| `FONT_TINY` | **30px** | יחידה ב-Goal Picker, Settings items |
| `FONT_SMALL` | **34px** | כותרות Settings, Alert line 2 |
| `FONT_MEDIUM` | **39px** | מרחק/צעדים (ערכים), Alert line 1 |
| `FONT_LARGE` | **42px** | לא בשימוש |
| `FONT_NUMBER_MILD` | **62px** | דופק (Heart Rate) |
| `FONT_NUMBER_MEDIUM` | **69px** | שעון (Time) |
| `FONT_NUMBER_HOT` | **107px** | יעד (Goal Picker number) |

### כלל 6: מיקומים - תמיד באחוזים!
```monkeyc
// ✅ נכון
var y = h / 8;                    // 12.5% מהגובה
var x = w / 2;                    // 50% מהרוחב
var padding = w / 14;             // 7.14% מהרוחב

// ❌ לא נכון
var y = 35;                       // פיקסלים קבועים - אסור!
var x = 140;                      // אסור!
```

### כלל 7: סדר תצוגה - **תווית לפני ערך!**
```
✅ נכון:
mi 0.00
steps 0
km 5.23

❌ לא נכון:
0.00 mi
0 steps
5.23 km
```

---

## 🌍 שפות ותרגומים - 6 שפות מלאות

### רשימת שפות:
| # | שפה | יחידות ברירת מחדל | קוד |
|---|-----|-------------------|-----|
| **0** | **English** | mi (מיילים) | en |
| **1** | **עברית** | km (קילומטרים) | he |
| **2** | **Español** | km | es |
| **3** | **Français** | km | fr |
| **4** | **Deutsch** | km | de |
| **5** | **中文** | km | zh |

### תרגומים מלאים:
| מפתח | English | עברית | Español | Français | Deutsch | 中文 |
|------|---------|-------|---------|----------|---------|------|
| **Settings** | Settings | הגדרות | Ajustes | Paramètres | Einstellungen | 设置 |
| **Language** | Language | שפה | Idioma | Langue | Sprache | 语言 |
| **Name** | Name | שם | Nombre | Nom | Name | 名称 |
| **Color** | Color | צבע | Color | Couleur | Farbe | 颜色 |
| **Save** | Save ✓ | שמור ✓ | Guardar ✓ | Sauvegarder ✓ | Speichern ✓ | 保存 ✓ |
| **START** | START | התחל | INICIAR | DÉMARRER | STARTEN | 开始 |
| **steps** | steps | צעדים | pasos | pas | Schritte | 步 |
| **target** | Target | יעד | Objetivo | Cible | Ziel | 目标 |
| **bpm** | bpm | דופק | ppm | bpm | bpm | 心率 |
| **auto** | Auto | אוטומטי | Auto | Auto | Auto | 自动 |
| **rest** | Rest / Slow down | תנוח / תאט | Descansa / Desacelera | Repos / Ralentir | Ruhe / Langsamer | 休息 / 放慢 |
| **continue** | You can continue | אתה יכול להמשיך | Puedes continuar | Tu peux continuer | Du kannst weitermachen | 你可以继续 |
| **continue2** | training | באימון | entrenando | l'entraînement | Training | 训练 |
| **greatJob** | Great job | כל הכבוד | ¡Buen trabajo! | Bon travail! | Gute Arbeit! | 做得好! |
| **halfway** | Halfway! | חצי דרך | ¡Mitad del camino! | À mi-chemin! | Halbzeit! | 半程! |
| **goalReached** | Goal reached! | גמרת את היעד | ¡Meta alcanzada! | Objectif atteint! | Ziel erreicht! | 目标达成! |
| **alreadyReached** | This goal | ליעד הזה | Esta meta | Cet objectif | Dieses Ziel | 此目标 |
| **alreadyReached2** | already reached | כבר הגעת | ya alcanzada | déjà atteint | bereits erreicht | 已达成 |

---

## ⌨️ מקלדת דינמית לפי שפה - **תכונה קריטית!**

### כלל חשוב: המקלדת משתנה אוטומטית בהתאם לשפה!

#### 🇬🇧 English (0):
```
שורה 1: P O I U Y T R E W Q
שורה 2: L K J H G F D S A
שורה 3: M N B V C X Z
שורה 4: ✓   ␣   ⌫
```
**מערך:** `["P","O","I","U","Y","T","R","E","W","Q","L","K","J","H","G","F","D","S","A","M","N","B","V","C","X","Z"]`

#### 🇮🇱 עברית (1):
```
שורה 1: ק ר א ט ו ן ם פ
שורה 2: ש ד ג כ ע י ח ל ך ף
שורה 3: ז ס ב ה נ מ צ ת ץ
שורה 4: ✓   ␣   ⌫
```
**מערך:** `["ק","ר","א","ט","ו","ן","ם","פ","ש","ד","ג","כ","ע","י","ח","ל","ך","ף","ז","ס","ב","ה","נ","מ","צ","ת","ץ"]`

#### 🇪🇸 Español (2):
```
שורה 1: P O I U Y T R E W Q
שורה 2: Ñ L K J H G F D S A
שורה 3: M N B V C X Z
שורה 4: ✓   ␣   ⌫
```
**תווים מיוחדים:** Ñ

#### 🇫🇷 Français (3):
```
שורה 1: P O I U Y T R E W Q
שורה 2: À L K J H G F D S A
שורה 3: M N B V C X Z É È
שורה 4: ✓   ␣   ⌫
```
**תווים מיוחדים:** À, É, È

#### 🇩🇪 Deutsch (4):
```
שורה 1: P O I U Y T R E W Q
שורה 2: Ü L K J H G F D S A
שורה 3: M N B V C X Z Ä Ö
שורה 4: ✓   ␣   ⌫
```
**תווים מיוחדים:** Ü, Ä, Ö

#### 🇨🇳 中文 (5):
```
שורה 1: 李 王 张 刘 陈 杨
שורה 2: 赵 黄 周 吴 徐 孙
שורה 3: 朱 马 胡 郭 林 何
שורה 4: ✓   ␣   ⌫
```

### איך זה עובד:
```monkeyc
function _buildKeyboard() {
    mKeyboardChars = [];
    
    if (mLanguage == 0) {
        // English
        mKeyboardChars = ["P","O","I",...];
    } else if (mLanguage == 1) {
        // Hebrew
        mKeyboardChars = ["ק","ר","א",...];
    }
    // ... כל השפות
}

function selectLanguage(idx) {
    mLanguage = idx;
    _buildKeyboard();  // ← בניית מקלדת מחדש!
    _saveSettings();
}
```

---

## 🎨 צבעים - 10 ערכות צבעים

### רשימת צבעים:
| # | שם | HEX | RGB | Garmin Constant |
|---|----|----|-----|-----------------|
| **0** | **Green** | `#00C853` | `0,200,83` | `Graphics.COLOR_GREEN` |
| **1** | **Cyan** | `#00FFFF` | `0,255,255` | `0x00FFFF` |
| **2** | **Blue** | `#2196F3` | `33,150,243` | `Graphics.COLOR_BLUE` |
| **3** | **Purple** | `#9C27B0` | `156,39,176` | `Graphics.COLOR_PURPLE` |
| **4** | **Red** | `#F44336` | `244,67,54` | `Graphics.COLOR_RED` |
| **5** | **Orange** | `#FF9800` | `255,152,0` | `Graphics.COLOR_ORANGE` |
| **6** | **Yellow** | `#FFEB3B` | `255,235,59` | `Graphics.COLOR_YELLOW` |
| **7** | **Pink** | `#E91E63` | `233,30,99` | `Graphics.COLOR_PINK` |
| **8** | **Lime** | `#BFFF00` | `191,255,0` | `0xBFFF00` |
| **9** | **White** | `#FFFFFF` | `255,255,255` | `Graphics.COLOR_WHITE` |

### איפה הצבע משפיע:
- ✅ שעון (זמן)
- ✅ אייקונים (🏃 👟 💚)
- ✅ סרגלי התקדמות
- ✅ כפתורים (START, OK, Save)
- ✅ טקסטים מודגשים

### קוד:
```monkeyc
function _getMainColor() {
    if (mColor == 0) { return Graphics.COLOR_GREEN; }
    if (mColor == 1) { return 0x00FFFF; }        // Cyan
    if (mColor == 2) { return Graphics.COLOR_BLUE; }
    // ... כל הצבעים
    return Graphics.COLOR_GREEN;
}
```

---

## 📱 מסך ראשי - Main Screen (State 0)

### פריסה מלמעלה למטה:
```
┌────────────────────────────────┐
│         08:15 (צבע)            │  ← שעון - FONT_NUMBER_MEDIUM (69px)
│                                │     tap → Settings
│  🏃 ▓▓▓▓▓  mi 0.00            │  ← מרחק - 39px ערך, 22px תווית
│                                │     tap → Goal Picker
│  👟 ▓▓▓▓▓  steps 0             │  ← צעדים - 39px ערך, 22px תווית
│                                │
│          72 💚                  │  ← דופק - FONT_NUMBER_MILD (62px)
└────────────────────────────────┘     tap → HR Selection
```

### אזורי מגע:
- **timeZone** (top 20%): tap → Settings (4)
- **distZone** (middle): tap → Goal Picker (1)
- **hrZone** (bottom): tap → HR Selection (2)

### סרגלי התקדמות:
- **5 חלקים מפולחים** (segments)
- רווח 3px בין חלקים
- end-cap 4px בצד ימין
- צבע מלא = מושלם | אפור כהה = לא מושלם

---

## 🎯 Goal Picker (State 1)

### מבנה:
```
┌────────────────────────────────┐
│            ▲                   │  ← tap → +1
│                                │
│   mi    5         [START]      │  ← יחידה 30px + מספר 107px
│                                │     tap → Main (0)
│            ▼                   │  ← tap → -1
└────────────────────────────────┘
```

### חשוב - יחידה נפרדת:
```monkeyc
// ✅ נכון - יחידה והמספר נפרדים
dc.drawText(x, y, Graphics.FONT_TINY, "mi", ...);           // 30px
dc.drawText(x + gap, y, Graphics.FONT_NUMBER_HOT, "5", ...); // 107px

// ❌ לא נכון - מחוברים
var str = "5 mi";
dc.drawText(x, y, Graphics.FONT_NUMBER_HOT, str, ...);
```

---

## 💓 HR Selection (State 2)

### מבנה:
```
┌────────────────────────────────┐
│            ▲                   │
│  Target: 140 bpm               │
│   💚  Auto         [OK]        │
│   50%                          │
│   55%, 60%, ..., 90%           │
│            ▼                   │
└────────────────────────────────┘
```

### אפשרויות:
- **Auto** (0): דופק נוכחי + 15
- **50%-90%** (1-9): אחוזים מדופק מקסימלי (190)

---

## ⚙️ Settings Screen (State 4)

### מבנה:
```
┌────────────────────────────────┐
│         Settings               │
│                                │
│  Language         EN           │  ← tap → Language (5)
│  Name             גבי          │  ← tap → Name (6)
│  Color            ■            │  ← tap → Color (7)
│                                │
│       [Save ✓]                 │  ← tap → Main (0)
└────────────────────────────────┘
```

---

## 🔔 התראות - Alert System

### סוגי התראות:

**1. דופק גבוה (HR High):**
```
┌────────────────────────────────┐
│    [שם] תנוח / תאט    (אדום)  │  ← FONT_MEDIUM (39px)
│                                │
└────────────────────────────────┘
```

**2. דופק תקין (HR OK):**
```
┌────────────────────────────────┐
│    אתה יכול להמשיך    (ירוק)  │  ← FONT_MEDIUM (39px)
│    באימון                      │  ← FONT_SMALL (34px)
└────────────────────────────────┘
```

**3. חצי דרך (Halfway):**
```
┌────────────────────────────────┐
│    כל הכבוד [שם]      (ירוק)  │
│    חצי דרך                     │
└────────────────────────────────┘
```

**4. השלמת יעד (Goal Reached):**
```
┌────────────────────────────────┐
│    כל הכבוד [שם]      (ירוק)  │
│    גמרת את היעד                │
└────────────────────────────────┘
```

**כל התראה:**
- ✅ רטט 500ms
- ✅ צליל (TONE_ALERT_HI)
- ✅ 5 שניות תצוגה
- ✅ חזרה אוטומטית למסך ראשי

---

## 🔄 State Machine - מכונת מצבים

### 8 מצבים:
| # | שם | תיאור |
|---|-----|--------|
| **0** | Main | מסך ראשי |
| **1** | Goal | בחירת יעד |
| **2** | HR | בחירת מצב דופק |
| **3** | Alert | התראה (5 שניות) |
| **4** | Settings | תפריט הגדרות |
| **5** | Language | בחירת שפה |
| **6** | Name | הקלדת שם |
| **7** | Color | בחירת צבע |

### מעברים עיקריים:
```
Main (0) → tap time → Settings (4)
Main (0) → tap distance → Goal (1)
Main (0) → tap HR → HR Selection (2)

Settings (4) → tap Language → Language (5) → select → Settings (4)
Settings (4) → tap Name → Name (6) → confirm → Settings (4)
Settings (4) → tap Color → Color (7) → select → Settings (4)
Settings (4) → tap Save → Main (0)

Goal (1) → tap START → Main (0)
HR (2) → tap OK → Alert (3) → after 5s → Main (0)
```

---

## 💾 Storage - שמירת נתונים

### מפתחות:
```monkeyc
KEY_GOAL_DIST           // יעד מרחק
KEY_HR_MODE             // מצב דופק (0-9)
KEY_ACTIVITY_STARTED    // פעילות פעילה?
KEY_START_STEPS         // צעדים התחלתיים
KEY_START_DIST          // מרחק התחלתי (cm)
KEY_HALFWAY_SHOWN       // הוצגה התראת חצי דרך?
KEY_GOAL_SHOWN          // הוצגה התראת יעד?
KEY_LANGUAGE            // שפה (0-5)
KEY_COLOR               // צבע (0-9)
KEY_USER_NAME           // שם משתמש
KEY_UNITS               // יחידות (0=km, 1=mi)
```

### שימוש:
```monkeyc
// שמירה
try {
    Application.Storage.setValue(KEY_LANGUAGE, mLanguage);
} catch(e) {}

// טעינה
try {
    var v = Application.Storage.getValue(KEY_LANGUAGE);
    if (v != null) { mLanguage = v; }
} catch(e) {}
```

---

## 📊 Data Sources - מקורות נתונים

### מרחק וצעדים:
```monkeyc
var info = ActivityMonitor.getInfo();
if (info != null) {
    var steps = info.steps;           // מספר צעדים
    var distanceCm = info.distance;   // מרחק ב-cm
}
```

### דופק:
```monkeyc
var actInfo = Activity.getActivityInfo();
if (actInfo != null && actInfo.currentHeartRate != null) {
    var hr = actInfo.currentHeartRate;  // דופק נוכחי
}
```

### שעון:
```monkeyc
var clockTime = System.getClockTime();
var hour = clockTime.hour;              // שעה (0-23)
var minute = clockTime.min;             // דקה (0-59)
```

### המרות:
```monkeyc
// Kilometers ↔ Centimeters
var cm = km * 100000;
var km = cm / 100000.0;

// Miles ↔ Centimeters
var cm = miles * 160934;
var miles = cm / 160934.0;
```

---

## 🐛 פתרון בעיות - Troubleshooting

### 1. "jungle file doesn't exist"
**סיבה:** אתה לא בתיקייה הנכונה
```cmd
cd fitbeat              # היכנס לתיקייה
dir                     # בדוק שיש monkey.jungle
```

### 2. "developer_key.der not found"
**סיבה:** הקובץ חסר
```cmd
dir                     # וודא שיש developer_key.der
```

### 3. תיקייה כפולה
**בעיה:** `C:\...\fitbeat\fitbeat\`
**פתרון:**
```cmd
cd fitbeat\fitbeat      # היכנס לתיקייה הפנימית
# או
move fitbeat\fitbeat\* fitbeat\  # העבר הכל רמה אחת למעלה
```

### 4. האפליקציה לא עובדת על השעון
- ✅ וודא Fenix 8 Solar 51mm
- ✅ אתחל את השעון
- ✅ בדוק Connect IQ מעודכן
- ✅ העתק ל-`GARMIN\APPS\`

### 5. פונטים נראים שגויים
**זו לא בעיה!**
- הפונטים של Garmin שונים מ-Arial
- הגדלים בפיקסלים נכונים
- על השעון האמיתי זה יראה נכון

---

## ✅ Checklist לפני Build

- [ ] אני בתיקיית `fitbeat/` (לא `fitbeat/fitbeat/`)
- [ ] יש `monkey.jungle` בתיקייה
- [ ] יש `manifest.xml` בתיקייה
- [ ] יש `developer_key.der` בתיקייה
- [ ] יש תיקיות `source/` ו-`resources/`
- [ ] הפקודה כוללת נתיב מלא ל-SDK
- [ ] המכשיר היעד הוא `fenix8solar51mm`

---

## 🔗 לינקים חשובים

### סימולציה:
```
https://fenix-preview.preview.emergentagent.com/fitbeat-interactive.html
```

### הורדת קוד:
```
https://fitness-tracker-643.preview.emergentagent.com/fitbeat_v4.3.0.zip
```

### דף הורדה מלא:
```
https://fitness-tracker-643.preview.emergentagent.com/fitbeat-download.html
```

---

## 🎯 סיכום - מה חשוב לזכור

1. **מבנה נכון** - תיקייה אחת `fitbeat/` (לא כפולה)
2. **פקודת Build** - מתוך התיקייה הנכונה
3. **פונטים מדויקים** - 69px, 39px, 22px, 62px
4. **6 שפות** - עם מקלדת דינמית
5. **10 צבעים** - לכל האלמנטים
6. **מיקומים יחסיים** - לא פיקסלים קבועים
7. **סדר נכון** - תווית לפני ערך (`mi 0.00`)
8. **סימולציה** - המקור האמיתי להתאמה

---

**קובץ תובנות משולב ומעודכן - FitBeat v4.3.1** ✅  
**כל מה שצריך במקום אחד!** 📱🚀
